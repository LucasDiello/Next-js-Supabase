/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['dl', 'www', 'dl.claid.ai', 'zgbombqfufsncxrjoltc.supabase.co', 'api.replicate.com', 'replicate.delivery'],
    },
};

export default nextConfig;
